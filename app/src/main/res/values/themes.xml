<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.NammaVastra_1" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Splash background -->
        <item name="android:windowBackground">@android:color/white</item>

    </style>

    <!-- App Theme -->
    <style name="Theme.NammaVastra_1" parent="Base.Theme.NammaVastra_1" />

</resources>